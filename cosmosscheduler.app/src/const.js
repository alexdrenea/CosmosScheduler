
export const API_URL = window.API_URL; //use the settings.js file in public to inject settings at build time (https://stackoverflow.com/questions/50369477/create-react-app-configuration-file-after-build-app)
export const API_HEADERS = {
    header: {
      'Content-Type': 'application/json'
    }
};